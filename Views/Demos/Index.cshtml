@model IEnumerable<Demo1234.Models.Demo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
             <th>
                @Html.DisplayNameFor(model => model.DemoID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DemoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DemoGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DemoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DemoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DemoGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DemoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DemoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DemoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
